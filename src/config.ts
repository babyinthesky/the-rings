const DOMAIN_URL = 'https://ringsdb.com';

const ERROR_DEFAULT_TEXT = 'Something went wrong. Please try it once more later on.';

const EMPTY_DATA_DEFAULT_TEXT = 'No data is loaded yet.';

export {
    DOMAIN_URL,
    ERROR_DEFAULT_TEXT,
    EMPTY_DATA_DEFAULT_TEXT,
};
